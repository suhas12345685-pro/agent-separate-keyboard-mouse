{
  "id": "agent-separate-keyboard-mouse",
  "name": "Agent Separate Keyboard & Mouse",
  "version": "1.0.0",
  "description": "Gives the AI agent its own isolated virtual keyboard and mouse. Controls a dedicated workspace window via Win32 PostMessage so it never steals focus from the user.",
  "mcp": {
    "command": "node",
    "args": ["dist/index.js"],
    "env": {
      "AGENT_WORKSPACE_TITLE": "Agent Workspace"
    }
  },
  "contracts": {}
}
