{
  "name": "agent-isolated-input",
  "version": "1.0.0",
  "description": "Isolated virtual keyboard and mouse for the AI agent — Win32 PostMessage via PowerShell bridge",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "openclaw": {
    "compat": {
      "pluginApi": ">=2026.5.30"
    },
    "build": {
      "openclawVersion": "2026.6.2"
    }
  }
}
